name: Discord Notifications

on:
  push:
    branches: [ "main" ]
  pull_request:
    types: [opened, closed]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord notification
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        with:
          args: "📢 [Hickory] Event: ${{ github.event_name }} | Repo: ${{ github.repository }} | Actor: ${{ github.actor }} | Ref: ${{ github.ref }}"
